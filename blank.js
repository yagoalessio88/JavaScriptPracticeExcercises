// tell if two words are anagrams

function myFunction() {}
// base cases
// if lenght of the strings doesn't match return false
// create two object with key "letter" and value "repetitions"
// compare the object properties
// sort the two strings and compare them
console.log(myFunction("danger", "garden"));

// given a sorted array of integers and a target integer, find the index of the first and last position of the target in arr. If target can't be found in arr, return [-1,-1]

// base cases
// if target not in arr return [-1,-1]
